/* global describe, it, expect */
import {func} from "../../src/index";

describe("autoupdate", () => {
  it("auto updates inserted documents", () => {
    expect(func()).toEqual(400);
  });

  it("auto updates inserted documents2", () => {
    expect(func()).toEqual(400);
  });
});

